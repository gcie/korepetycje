import { LessonsModePipe } from './lessons-mode.pipe';

describe('LessonsModePipe', () => {
  it('create an instance', () => {
    const pipe = new LessonsModePipe();
    expect(pipe).toBeTruthy();
  });
});
